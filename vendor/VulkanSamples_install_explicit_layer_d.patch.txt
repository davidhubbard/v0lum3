diff --git a/layers/CMakeLists.txt b/layers/CMakeLists.txt
index 076b847..7f87777 100644
--- a/layers/CMakeLists.txt
+++ b/layers/CMakeLists.txt
@@ -62,6 +62,11 @@ else()
                 )
         endforeach(config_file)
     endif()
+    foreach (config_file ${LAYER_JSON_FILES})
+        install (FILES ${CMAKE_CURRENT_SOURCE_DIR}/linux/${config_file}.json
+            DESTINATION etc/vulkan/explicit_layer.d
+            )
+    endforeach(config_file)
 endif()
 
 if (WIN32)
@@ -80,8 +85,11 @@ else()
     add_library(VkLayer_${target} SHARED ${ARGN})
     target_link_Libraries(VkLayer_${target} VkLayer_utils)
     add_dependencies(VkLayer_${target} generate_vk_layer_helpers)
-    set_target_properties(VkLayer_${target} PROPERTIES LINK_FLAGS "-Wl,-Bsymbolic")
+    set_target_properties(VkLayer_${target}
+        PROPERTIES LINK_FLAGS "-Wl,-Bsymbolic"
+            INSTALL_RPATH ${CMAKE_INSTALL_PREFIX}/etc/vulkan/explicit_layer.d )
     install(TARGETS VkLayer_${target} DESTINATION ${PROJECT_BINARY_DIR}/install_staging)
+    install(TARGETS VkLayer_${target} DESTINATION etc/vulkan/explicit_layer.d)
     endmacro()
 endif()
 
@@ -149,6 +157,7 @@ if (WIN32)
 else()
     add_library(VkLayer_utils SHARED vk_layer_config.cpp vk_layer_extension_utils.cpp vk_layer_utils.cpp)
     install(TARGETS VkLayer_utils DESTINATION ${PROJECT_BINARY_DIR}/install_staging)
+    install(TARGETS VkLayer_utils DESTINATION etc/vulkan/explicit_layer.d)
 endif()
 
 add_vk_layer(core_validation core_validation.cpp vk_layer_table.cpp vk_safe_struct.cpp descriptor_sets.cpp)
