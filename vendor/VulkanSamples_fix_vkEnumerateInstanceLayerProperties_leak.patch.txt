diff --git a/loader/loader.c b/loader/loader.c
index 2400ae3..4f8b1d6 100644
--- a/loader/loader.c
+++ b/loader/loader.c
@@ -1130,6 +1130,18 @@ VkResult loader_add_to_layer_list(const struct loader_instance *inst,
 
         memcpy(&list->list[list->count], layer,
                sizeof(struct loader_layer_properties));
+        // list->list[list->count].instance_extension_list.list now references
+        // the same memory as layer->instance_extension_list. A ref-counter
+        // or a deep copy is needed at this point.
+        memset(&list->list[list->count].instance_extension_list, 0,
+                sizeof(list->list[list->count].instance_extension_list));
+        VkResult result = loader_add_to_ext_list(
+            inst, &list->list[list->count].instance_extension_list,
+            layer->instance_extension_list.count,
+            layer->instance_extension_list.list);
+        if (result != VK_SUCCESS) {
+            return result;
+        }
         list->count++;
     }
 
diff --git a/loader/trampoline.c b/loader/trampoline.c
index 81513f9..865ae2f 100644
--- a/loader/trampoline.c
+++ b/loader/trampoline.c
@@ -157,7 +157,7 @@ vkEnumerateInstanceExtensionProperties(const char *pLayerName,
                 loader_add_to_ext_list(NULL, &local_ext_list, ext_list->count,
                                        ext_list->list);
             }
-            loader_destroy_layer_list(NULL, NULL, &local_list);
+            loader_delete_layer_properties(NULL, &local_list);
             global_ext_list = &local_ext_list;
 
         } else {
@@ -244,7 +244,7 @@ vkEnumerateInstanceLayerProperties(uint32_t *pPropertyCount,
 
     if (pProperties == NULL) {
         *pPropertyCount = instance_layer_list.count;
-        loader_destroy_layer_list(NULL, NULL, &instance_layer_list);
+        loader_delete_layer_properties(NULL, &instance_layer_list);
         return VK_SUCCESS;
     }
 
@@ -259,11 +259,11 @@ vkEnumerateInstanceLayerProperties(uint32_t *pPropertyCount,
     *pPropertyCount = copy_size;
 
     if (copy_size < instance_layer_list.count) {
-        loader_destroy_layer_list(NULL, NULL, &instance_layer_list);
+        loader_delete_layer_properties(NULL, &instance_layer_list);
         return VK_INCOMPLETE;
     }
 
-    loader_destroy_layer_list(NULL, NULL, &instance_layer_list);
+    loader_delete_layer_properties(NULL, &instance_layer_list);
 
     return VK_SUCCESS;
 }
